<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Location | LocaLink</title>
    <link rel="stylesheet" href="../style.css" />
  </head>
  <body>
    <header>
      <h2 class="logo">LocaLink</h2>
      <nav class="navigation">
        <a href="/home-employer">Home</a>
        <a href="#">About</a>
        <a href="/logout">Logout</a>
        <!-- Add a logout link -->
      </nav>
    </header>
    <main>
        <fieldset id="potential-workers-list" class="input-box" name="potential-workers">
            <span>
                Here's a list of potential workers for <%= skillname %>
            </span>
            <ul>
                <% workers.forEach( worker => { %>
                    <li><%= worker.firstname %> <%= worker.lastname %> - <%= worker.experience %> years of experience - $<%=worker.preferredrate%> an hour</li>
                <% }); %>
            </ul>

        </fieldset>
    </main>
  </body>

</html>
